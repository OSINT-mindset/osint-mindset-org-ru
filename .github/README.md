# GitHub Actions Configuration

## Автоматический деплой на GitHub Pages

Файл `workflows/ci.yml` настроен для автоматической сборки и публикации сайта.

### Как это работает

1. **Триггер:** Пуш в ветку `main` или `master`
2. **Сборка:**
   - Устанавливается Python 3.x
   - Устанавливаются зависимости из `requirements.txt`
   - Выполняется `mkdocs build`
3. **Деплой:** Результат публикуется на GitHub Pages

### Настройка репозитория

**Один раз нужно настроить в GitHub:**

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите **GitHub Actions**
3. Сохраните

После этого при каждом пуше в `main` сайт будет автоматически обновляться.

### Ручной запуск

Можно запустить деплой вручную:

1. Перейдите в **Actions** → **Deploy to GitHub Pages**
2. Нажмите **Run workflow**
3. Выберите ветку и нажмите **Run workflow**

### Проверка статуса

- Зелёная галочка ✅ — деплой успешен
- Красный крестик ❌ — ошибка при сборке
- Жёлтый кружок 🟡 — деплой в процессе

Кликните на статус, чтобы увидеть логи.

### Требования

- Python 3.x
- Все зависимости указаны в `requirements.txt`
- Конфигурация в `mkdocs.yml` корректна

### Время сборки

Обычно занимает 1-2 минуты:
- Установка зависимостей: ~30 сек
- Сборка сайта: ~2 сек
- Публикация: ~30 сек

### Troubleshooting

**Ошибка при сборке:**
1. Проверьте логи в Actions
2. Запустите `mkdocs build` локально
3. Убедитесь, что все зависимости в `requirements.txt`

**Сайт не обновляется:**
1. Проверьте, что Source в Settings → Pages установлен на "GitHub Actions"
2. Проверьте статус последнего workflow в Actions
3. Очистите кеш браузера (Ctrl+F5)

**Permissions error:**
Убедитесь, что в `ci.yml` указаны правильные permissions:
```yaml
permissions:
  contents: read
  pages: write
  id-token: write
```

